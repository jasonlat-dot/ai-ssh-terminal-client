# Full SSH controller URL.
VITE_SSH_API_BASE_URL=http://localhost:8888/api/v1/ssh
# Full AgentController URL (the current controller is mapped to /agent).
VITE_AGENT_API_BASE_URL=http://localhost:8888/agent
# Explicitly shared by list and create (backend defaults differ).
VITE_SSH_USER_ID=default
